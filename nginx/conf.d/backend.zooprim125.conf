server {
    listen 8000;

    server_name _;

    location / {
        proxy_set_header Host $http_host;
        proxy_pass http://smm_shelter-backend:8000;
    }

    location /storage {
        alias /storage/;
    }
}